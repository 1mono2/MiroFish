# Render TODO

## 方針

- Render を単一プラットフォームとして使う
- 想定用途は身内向けの共有サービス
- 現時点では「最短で共有できること」を優先し、本格的な本番運用対策は一部見送る

## 実施すること

- [ ] 永続データの保存先を環境変数で切り替えられるようにする
  - 例: `UPLOAD_FOLDER`, `LOG_DIR`, `OASIS_SIMULATION_DATA_DIR`, `TASKS_DIR`
  - ローカル開発時は現行パスをデフォルトにしつつ、Render では Persistent Disk の mount path を指定できるようにする

- [ ] フロントエンドの API URL を本番用に設定できるようにする
  - `VITE_API_BASE_URL` を Render 側で設定する
  - フロントから `localhost:5001` に固定で飛ばないようにする

- [ ] `TaskManager` をメモリだけでなく永続化する
  - 再起動・再デプロイ後もタスク状態を読めるようにする
  - ファイルベースで十分なら JSON 保存で進める

- [ ] 長時間ジョブの状態をファイルまたは DB に保存する
  - シミュレーション準備
  - レポート生成
  - 再起動後も polling API から状態確認できるようにする

- [ ] Docker を使う場合は Dockerfile を本番向けに直す
  - 現在の `npm run dev` 前提をやめる
  - 開発用の前後同時起動ではなく、Render 配備に合わせた起動構成にする

## 今は見送ること

- [ ] バックエンドを開発サーバーから本番サーバーへ差し替える
  - 身内向けのため現時点では見送り

- [ ] リクエスト本文のログ出力停止
  - 身内向けのため現時点では見送り

- [ ] 認証の追加
  - 必要性はあるが、実装コスト優先で現時点では見送り

- [ ] SPA ルーティング対策
  - 現時点では見送り
  - ただし、Vue Router は history モードなので、直接URLアクセスやリロード時に問題が出る可能性がある

- [ ] シミュレーション削除機能や保持期限の追加
  - 現時点では見送り

## 実装順の目安

1. 永続パスの env 化
2. `TaskManager` の永続化
3. 長時間ジョブ状態の永続化
4. フロントの `VITE_API_BASE_URL` 対応
5. Docker を使うなら Dockerfile 調整

